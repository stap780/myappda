<nav class="navbar navbar-expand-md navbar-dark py-0 ">
  <a class="navbar-brand" href="<%= root_path %>">
    <%= image_tag "new_logo_myappda_white.svg", data: {src: "new_logo_myappda_white.svg", title: 'MyAppDa'}, class: 'logo logo_white' %>
  </a>
<button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
  <div class="collapse navbar-collapse" id="navbarColor02">
    <ul class="navbar-nav me-auto">
      <li class="nav-item <%= 'active' if current_page?(dashboard_path) %>">
        <a class="nav-link" href="<%= dashboard_path %>">Главная</a>
      </li>
      <%= render "/layouts/admin_navigation" if current_admin %>
      <%= render "/layouts/user_navigation" unless current_admin %>
    </ul>

    <% if current_user != true_user %>
    <ul class="navbar-nav mx-auto">
      <%= button_to "You (#{true_user.name }) are signed in as #{current_user.name }<br>Back to admin".html_safe, stop_impersonating_users_path, data: {turbo: false}, class: "btn text-white"  %>
    </ul>
    <% end %>

    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <% if current_user %>
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#">
            <i class="fa fa-user-circle" aria-hidden="true"></i>
            <%= current_user.name %>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu dropdown-user">
            <li><%= link_to "Выйти", destroy_user_session_url, data: { turbo_method: :delete }, class: 'text-decoration-none' %></li>
          </ul>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
